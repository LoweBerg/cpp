#include <iostream>
#include <cmath>
#include <vector>
#include <tuple>

using namespace std;

int main() {
    int m, n;
    cin >> m;
    cin >> n;

    int p1 = sqrt(m) + 1;
    int p2 = sqrt(m) + 1;
    int r;
    vector<tuple<int, int>> ans;

    for(int i = 1; i < sqrt(n) + 1; i++) {

    }
}