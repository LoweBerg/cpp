#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    int n, l;
    cin >> n >> l;

    vector<int> q(n);

    vector<int> dp;

    for(int &e : q) {
        cin >> e;
    }

    int f[4] = {++l, l, l, l--};

    for(int i : q) {
        
    }
}